[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sanyuan-hive"
version = "0.1.0a1"
description = "Sanyuan Hive Main — hive_id + B_T 单任务多通道并行解算运行域"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
hive = "sanyuan_hive.cli:main"
